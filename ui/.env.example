# Base URL of the FastAPI backend (REST). Include trailing slash.
VITE_API_URL=https://nova-orchestrator-api.onrender.com/

# WebSocket URL of the FastAPI backend (note wss:// for HTTPS hosts).
VITE_WS_URL=wss://nova-orchestrator-api.onrender.com/ws
